# BRScan SKey GUI 🖨️

เครื่องมือ GUI สำหรับจัดการการตั้งค่าปุ่มสแกนของ Brother Scanner บน Linux (brscan-skey) พัฒนาด้วย **C++ และ Qt6 Widgets** เพื่อให้ทำงานเข้ากับ Theme ของ OS ได้ 100%

## 📋 รายละเอียดโครงการ

`brscan-skey-gui` เป็นโปรแกรมที่ช่วยให้ผู้ใช้สามารถปรับแต่งค่าคอนฟิกูเรชันของ `brscan-skey` ผ่านหน้าจอที่ใช้งานง่าย แทนการแก้ไขไฟล์ข้อความโดยตรง โปรแกรมนี้รองรับการจัดการไฟล์คอนฟิกหลักทั้ง 4 รูปแบบ:

- `scantofile.config`
- `scantoemail.config`
- `scantoimage.config`
- `scantoocr.config`

## ✨ คุณสมบัติหลัก

- **Native Look & Feel**: พัฒนาด้วย C++ Qt6 Widgets ที่แสดงผลกลมกลืนกับ Theme ของ OS 100% (GNOME, KDE, etc.)
- **Auto-Config Creation**: ตรวจสอบและสร้างไฟล์คอนฟิกพื้นฐานให้อัตโนมัติหากไม่พบในระบบ
- **Configuration Management**: ความละเอียด (DPI), การสแกนสองด้าน (Duplex), ขนาดกระดาษ (Size)
- **รองรับทุก Linux Distro**: Debian, Ubuntu, Fedora, Arch Linux, Manjaro, etc.

## 🛠️ ความต้องการของระบบ (Requirements)

- **Linux OS** (Debian 12+, Ubuntu 22.04+, Fedora 38+, Arch Linux)
- **Qt6** >= 6.2
- **brscan-skey** (โปรแกรมควบคุมปุ่มสแกนของ Brother)

## 🚀 การติดตั้ง (Installation)

### วิธีที่ 1: ติดตั้งจากไฟล์ .deb (Debian / Ubuntu)

ดาวน์โหลดไฟล์ `.deb` จากหน้า [Releases](https://github.com/chainarong-ck/brscan-skey-gui/releases) แล้วติดตั้ง:

```bash
sudo dpkg -i brscan-skey-gui-0.1.0-Linux.deb
sudo apt install -f  # ติดตั้ง dependencies ที่ขาด (ถ้ามี)
```

โปรแกรมจะปรากฏในเมนูของระบบ หรือเรียกใช้ผ่าน Terminal: `brscan-skey-gui`

---

### วิธีที่ 2: Build จาก Source Code (สำหรับ Developer)

#### Build Dependencies

**Debian / Ubuntu:**

```bash
sudo apt install cmake g++ qt6-base-dev
```

**Fedora:**

```bash
sudo dnf install cmake gcc-c++ qt6-qtbase-devel
```

**Arch Linux / Manjaro:**

```bash
sudo pacman -S cmake gcc qt6-base
```

#### Build

```bash
mkdir build && cd build
cmake ..
make -j$(nproc)
```

#### รันโปรแกรม

```bash
./brscan-skey-gui
```

#### สร้างไฟล์ .deb / .rpm

```bash
# ภายใน build directory
cpack -G DEB   # สร้างไฟล์ .deb
cpack -G RPM   # สร้างไฟล์ .rpm
```

## 📂 ตำแหน่งไฟล์คอนฟิก

โปรแกรมจะทำงานกับไฟล์ที่อยู่ในไดเรกทอรี:
`~/.brscan-skey/`

## แผนการพัฒนาในอนาคต

- [ ] เพิ่มระบบจัดการ Profile หลายตัวสำหรับการสแกน
- [ ] พัฒนาหน้าจอ Preview สำหรับการทดสอบการสแกน
- [ ] รองรับการทำงานใน System Tray

## 📄 ใบอนุญาต (License)

โครงการนี้อยู่ภายใต้ใบอนุญาต [LICENSE](LICENSE)

---
> **หมายเหตุ:** โปรแกรมนี้เป็นเครื่องมือสนับสนุนสำหรับผู้ใช้ Brother Scanner บน Linux
