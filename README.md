# BRScan SKey GUI 🖨️

เครื่องมือ GUI สำหรับจัดการการตั้งค่าปุ่มสแกนของ Brother Scanner บน Linux (brscan-skey) พัฒนาด้วย Python และ PySide6 (Qt6)

## 📋 รายละเอียดโครงการ

`brscan-skey-gui` เป็นโปรแกรมที่ช่วยให้ผู้ใช้สามารถปรับแต่งค่าคอนฟิกูเรชันของ `brscan-skey` ผ่านหน้าจอที่ใช้งานง่าย แทนการแก้ไขไฟล์ข้อความโดยตรง โปรแกรมนี้รองรับการจัดการไฟล์คอนฟิกหลักทั้ง 4 รูปแบบ:

- `scantofile.config`
- `scantoemail.config`
- `scantoimage.config`
- `scantoocr.config`

## ✨ คุณสมบัติหลัก

- **Interface ใช้งานง่าย**: พัฒนาด้วย Qt6 ที่รองรับ High DPI และการแสดงผลที่คมชัด
- **Auto-Config Creation**: ตรวจสอบและสร้างไฟล์คอนฟิกพื้นฐานให้อัตโนมัติหากไม่พบในระบบ
- **Configuration Management**: ความละเอียด (DPI), การสแกนสองด้าน (Duplex), ขนาดกระดาษ (Size)

## 🛠️ ความต้องการของระบบ (Requirements)

- **Linux OS** (เช่น Arch Linux, Fedora, Ubuntu)
- **Python** >= 3.10
- **brscan-skey** (โปรแกรมควบคุมปุ่มสแกนของ Brother)
- **PySide6** (Qt6 for Python)

## 🚀 การติดตั้ง (Installation)

### วิธีที่ 1: ติดตั้งผ่าน Script อัตโนมัติ (แนะนำสำหรับผู้ใช้ทั่วไป)

เรามีสคริปต์ `install.sh` ที่จะตรวจสอบระบบของคุณและเลือกวิธีติดตั้งที่ดีที่สุดให้ (รองรับ Ubuntu, Debian, Fedora, Arch):

```bash
# ให้สิทธิ์ Execute
chmod +x install.sh
# รันตัวติดตั้ง
./install.sh
```

เมื่อติดตั้งเสร็จแล้ว คุณสามารถเรียกโปรแกรมได้จากเมนูหรือพิมพ์ `brscan-skey-gui` ใน Terminal

---

### วิธีที่ 2: ติดตั้งด้วยตัวเอง (Manual)

#### 🐧 Debian 13+ / Fedora / Arch (System Packages)

ระบบเหล่านี้มี PySide6 ให้ติดตั้งผ่าน Package Manager ซึ่งจะทำงานได้เร็วและเข้ากับระบบที่สุด

**Debian 13+:**

```bash
sudo apt install python3-pyside6.qtcore python3-pyside6.qtwidgets
```

**Fedora:**

```bash
sudo dnf install python3-pyside6
```

**Arch Linux / Manjaro:**

```bash
sudo pacman -S pyside6
```

หลังจากติดตั้ง Dependencies แล้ว ให้รันโดยตรง:

```bash
python3 src/main.py
```

#### 🟠 Ubuntu 24.04 (Noble) และระบบอื่นๆ

เนื่องจาก Ubuntu 24.04 ไม่มีแพ็กเกจ PySide6 ในคลังทางการ เราแนะนำให้ใช้ `uv` หรือ `venv`:

**แบบใช้ uv (แนะนำ):**

```bash
# ติดตั้ง uv (ถ้ายังไม่มี)
curl -LsSf https://astral.sh/uv/install.sh | sh

# รันโปรแกรม
uv run brscan-skey-gui
```

**แบบใช้ pip (Standard):**

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt  # หรือ pip install pyside6
python3 src/main.py
```

## 📂 ตำแหน่งไฟล์คอนฟิก

โปรแกรมจะทำงานกับไฟล์ที่อยู่ในไดเรกทอรี:
`~/.brscan-skey/`

## แผนการพัฒนาในอนาคต

- [ ] เพิ่มระบบจัดการ Profile หลายตัวสำหรับการสแกน
- [ ] พัฒนาหน้าจอ Preview สำหรับการทดสอบการสแกน
- [ ] รองรับการทำงานใน System Tray

## 📄 ใบอนุญาต (License)

โครงการนี้อยู่ภายใต้ใบอนุญาต [LICENSE](LICENSE)

---
> **หมายเหตุ:** โปรแกรมนี้เป็นเครื่องมือสนับสนุนสำหรับผู้ใช้ Brother Scanner บน Linux
