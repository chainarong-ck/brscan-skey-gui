# BRScan SKey GUI 🖨️

เครื่องมือ GUI สำหรับจัดการการตั้งค่าปุ่มสแกนของ Brother Scanner บน Linux (brscan-skey) พัฒนาด้วย Python และ PySide6 (Qt6)

## 📋 รายละเอียดโครงการ

`brscan-skey-gui` เป็นโปรแกรมที่ช่วยให้ผู้ใช้สามารถปรับแต่งค่าคอนฟิกูเรชันของ `brscan-skey` ผ่านหน้าจอที่ใช้งานง่าย แทนการแก้ไขไฟล์ข้อความโดยตรง โปรแกรมนี้รองรับการจัดการไฟล์คอนฟิกหลักทั้ง 4 รูปแบบ:

- `scantofile.config`
- `scantoemail.config`
- `scantoimage.config`
- `scantoocr.config`

## ✨ คุณสมบัติหลัก

- **Interface ใช้งานง่าย**: พัฒนาด้วย Qt6 ที่รองรับ High DPI และการแสดงผลที่คมชัด
- **Auto-Config Creation**: ตรวจสอบและสร้างไฟล์คอนฟิกพื้นฐานให้อัตโนมัติหากไม่พบในระบบ
- **Configuration Management**: ความละเอียด (DPI), การสแกนสองด้าน (Duplex), ขนาดกระดาษ (Size)

## 🛠️ ความต้องการของระบบ (Requirements)

- **Linux OS** (เช่น Arch Linux, Fedora, Ubuntu)
- **Python** >= 3.10
- **brscan-skey** (โปรแกรมควบคุมปุ่มสแกนของ Brother)
- **PySide6** (Qt6 for Python)

## 🚀 การติดตั้งและใช้งาน

### 1. โคลนโปรเจกต์

```bash
git clone https://github.com/chainarong-ck/brscan-skey-gui.git
cd brscan-skey-gui
```

### 2. ตั้งค่าสภาพแวดล้อม (แนะนำให้ใช้ `uv`)

หากคุณมี `uv` ติดตั้งอยู่แล้ว:

```bash
uv sync
```

หรือใช้ `venv` ปกติ:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e .
```

### 3. เริ่มใช้งานโปรแกรม

หากใช้ `uv`

```bash
uv run brscan-skey-gui
```

หากใช้ `venv`

```bash
source .venv/bin/activate
brscan-skey-gui
```

หรือเรียกใช้งานโดยตรง

```bash
python3 src/main.py
```

## 📂 ตำแหน่งไฟล์คอนฟิก

โปรแกรมจะทำงานกับไฟล์ที่อยู่ในไดเรกทอรี:
`~/.brscan-skey/`

## แผนการพัฒนาในอนาคต

- [ ] เพิ่มระบบจัดการ Profile หลายตัวสำหรับการสแกน
- [ ] พัฒนาหน้าจอ Preview สำหรับการทดสอบการสแกน
- [ ] รองรับการทำงานใน System Tray

## 📄 ใบอนุญาต (License)

โครงการนี้อยู่ภายใต้ใบอนุญาต [LICENSE](LICENSE)

---
> **หมายเหตุ:** โปรแกรมนี้เป็นเครื่องมือสนับสนุนสำหรับผู้ใช้ Brother Scanner บน Linux
